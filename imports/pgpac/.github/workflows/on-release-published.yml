name: Release
run-name: Release ${{ github.ref_name }}

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: write

concurrency:
  group: release-publish-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  sign-and-notarize:
    runs-on: macos-15
    steps:
      - name: Download unsigned macOS artifacts
        uses: robinraju/release-downloader@v1.11
        with:
          tag: ${{ github.ref_name }}
          fileName: 'pgpac-*-darwin-arm64-unsigned.zip'

      - name: Extract binaries
        run: |
          set -euo pipefail
          mkdir -p .out/darwin/arm64
          unzip -o -d .out/darwin/arm64 pgpac-${{ github.ref_name }}-darwin-arm64-unsigned.zip
          test -f .out/darwin/arm64/pgpac

      - name: Add cert to keychain
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.MAC_CERT_BASE64 }}
          p12-password: ${{ secrets.MAC_CERT_PASSWORD }}

      - name: Sign binaries
        run: |
          set -euo pipefail
          expected_identifier="io.magnusopera.pgpac.arm64"
          codesign \
            --force \
            --timestamp \
            --options=runtime \
            --identifier "$expected_identifier" \
            --sign "Developer ID Application: Magnus Opera (${{ secrets.MAC_DEV_TEAM_ID }})" \
            .out/darwin/arm64/pgpac
          codesign --verify --verbose=4 .out/darwin/arm64/pgpac
          actual_identifier="$(codesign -dv --verbose=4 .out/darwin/arm64/pgpac 2>&1 | sed -n 's/^Identifier=//p')"
          if [ "$actual_identifier" != "$expected_identifier" ]; then
            echo "Expected codesign identifier '$expected_identifier' but found '$actual_identifier'" >&2
            exit 1
          fi
          (cd .out/darwin/arm64; zip -q -r ../pgpac-${{ github.ref_name }}-darwin-arm64.zip ./pgpac)

      - name: Notarize arm64
        uses: GuillaumeFalourd/notary-tools@v1
        timeout-minutes: 10
        with:
          product_path: '.out/darwin/pgpac-${{ github.ref_name }}-darwin-arm64.zip'
          apple_id: ${{ secrets.MAC_DEV_LOGIN }}
          password: ${{ secrets.MAC_DEV_PASSWORD }}
          team_id: ${{ secrets.MAC_DEV_TEAM_ID }}
          xcode_path: '/Applications/Xcode_16.4.app'
          staple: false

      - name: Upload signed macOS artifacts
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            .out/darwin/pgpac-${{ github.ref_name }}-darwin-arm64.zip

      - name: Remove unsigned macOS artifacts
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            try {
              const tag = context.ref.replace('refs/tags/', '');
              const unsignedNames = [
                `pgpac-${tag}-darwin-arm64-unsigned.zip`,
              ];
              const { data: release } = await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag,
              });
              for (const unsignedName of unsignedNames) {
                const asset = release.assets.find(a => a.name === unsignedName);
                if (!asset) {
                  core.info(`Unsigned artifact not found: ${unsignedName}`);
                  continue;
                }
                await github.rest.repos.deleteReleaseAsset({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  asset_id: asset.id,
                });
                core.info(`Deleted unsigned artifact: ${unsignedName}`);
              }
            } catch (error) {
              core.warning(`Best-effort cleanup failed: ${error?.message ?? error}`);
            }

  update-homebrew-tap:
    uses: ./.github/workflows/release-homebrew-tap.yml
    needs:
      - sign-and-notarize
    with:
      ref: ${{ github.ref }}
      version: ${{ github.ref_name }}
      dry-run: false
    secrets: inherit
